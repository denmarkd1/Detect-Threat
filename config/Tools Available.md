Tools Available

Security sweep: Scan now (one-time full scan), Start/Stop active scan mode (continuous foreground watchdog), View details for security score/tier.
Security fixes: Fix notifications, Fix overlay, Check for app updates.
Incident lifecycle: Start incident work, Resolve next, Reopen latest.
Wi-Fi posture module: Run posture scan, View findings.
Anti-phishing module: Scan link/text, View guardian feed, phishing result remediation flow.
Credential Defense Center: Edit identity, Link primary email, Scan breaches (k-anonymity breach checks), queue/rotation management.
Service password action popup (inside Credential Center): Generate local strong password, Copy generated password, Copy + open password change, Keep current password (save local), Change + queue rotation, Start overlay assistant, Copy stored current password, Copy previous password, I changed it, set new as current.
Queue controls: Refresh queue, filter by status (all/pending/overdue/completed), owner filter (all/parent/child via long press).
Autofill/passkey tools: Open autofill settings, Open passkey settings.
Encrypted Media Vault: Add to vault, Open vault, item actions Secure open, Export item, Delete item, Restore item.
Device locator tools: Open provider, Provider setup, provider picker/setup guidance.
AI Copilot tools: Refresh copilot, Open playbook, Manage connection (link provider/model, enter session API key, refresh, disconnect).
Hygiene toolkit: Run hygiene audit, Apply safe cleanup.
Language/translation: Open language settings, Report translation issue.
Plans/support: Choose plan, Refresh purchase status, Rate + Refer, Open billing page, support center link.
Bottom nav shortcuts: Sweep, Threats, Credentials, Services.
Settings Available (Guardian Settings Hub)

Appearance + identity: theme shade, theme tone (light/dark), lion fill mode, lion profile (male/female/non-binary), palette preview toggles.
Profile override: Allow child lions as main profiles (Pro) toggle.
Regional testing override: Set US state override (2-letter code), plus clear/reset to Auto.
Startup intro settings: Replay full intro on every app launch, Enable widget and nav startup motion.
Global settings routes: open plan/billing settings, AI connection settings, language settings, locator setup, and credential center.
System-Level Permissions/Settings Hooks Used

Notifications permission.
Overlay permission (SYSTEM_ALERT_WINDOW).
Wi-Fi + location permissions (NEARBY_WIFI_DEVICES, ACCESS_FINE/COARSE_LOCATION).
Foreground service + boot receiver for continuous watchdog behavior.
Biometric/fingerprint support paths present in APK components.

